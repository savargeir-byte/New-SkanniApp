*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Keystore files (keep in repo for now)
# keystore.properties - removed to allow keystores in repo
# notescanner-release.jks - removed to allow keystores in repo  
# upload-keystore.jks - removed to allow keystores in repo
upload-keystore.jks.b64

# APK and AAB files
*.apk
*.aab
app-release.aab

# Build artifacts and logs
gradle-build*.log
aapt2-link-output.txt
android-signing.env

# Status files
BUILD_STATUS.md
DEPLOYMENT_READY.md
GITHUB_RELEASE_COMPLETE.md
HYBRID_OCR_COMPLETE.md
OCR_ENHANCEMENT_STATUS.md
PLAY_SUBMISSION.md

# Release notes (keep current only)
RELEASE_NOTES_v*.md

# Local secrets and helper artifacts (do not commit)
.secrets/
scripts/*.local.*

# Android app build outputs (custom build dirs)
# Do NOT commit build artifacts from Gradle builds
app/build/
app/build_*/
app/build_android*/
app/build_android3/
app/build_android4/
app/build_release_*/
app/build_ocr_*/
app/**/intermediates/
app/**/generated/
app/**/outputs/

# Local release artifact bundles created for convenience
release_artifacts_*/
release_artifacts/

# Temporary directories
powershell/
java/
winget/

# HTML files (unless documentation)
index.html
